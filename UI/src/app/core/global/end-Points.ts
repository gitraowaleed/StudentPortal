const StudentsRoot = 'Students';
const Students = {
    root: `${StudentsRoot}`,
    addUpdate: `${StudentsRoot}/AddUpdate`,
    getAll: `${StudentsRoot}/GetAll`
};

const CoursesRoot = 'Courses';
const Courses = {
    root: `${CoursesRoot}`,
};

const TeachersRoot = 'Teachers';
const Teachers =
{
    root: `${TeachersRoot}`,
}


export const endPoints = {
    students: Students,
    teachers: Teachers,
    courses: Courses,
};